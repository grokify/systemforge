// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/grokify/coreforge/identity/ent/agent"
	"github.com/grokify/coreforge/identity/ent/principal"
)

// Agent is the model entity for the Agent schema.
type Agent struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Reference to parent Principal
	PrincipalID uuid.UUID `json:"principal_id,omitempty"`
	// AI model identifier (e.g., claude-3-opus)
	ModelID string `json:"model_id,omitempty"`
	// Agent version
	Version string `json:"version,omitempty"`
	// Principal who delegated authority to this agent
	DelegatingPrincipalID *uuid.UUID `json:"delegating_principal_id,omitempty"`
	// Capabilities this agent is constrained to
	CapabilityConstraints []string `json:"capability_constraints,omitempty"`
	// Resources this agent can access
	ResourceConstraints []string `json:"resource_constraints,omitempty"`
	// Maximum token lifetime in seconds (default 1 hour)
	MaxTokenLifetime int `json:"max_token_lifetime,omitempty"`
	// Current session identifier
	SessionID *string `json:"session_id,omitempty"`
	// Whether actions require human confirmation
	RequiresConfirmation bool `json:"requires_confirmation,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AgentQuery when eager-loading is set.
	Edges        AgentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AgentEdges holds the relations/edges for other nodes in the graph.
type AgentEdges struct {
	// Principal holds the value of the principal edge.
	Principal *Principal `json:"principal,omitempty"`
	// Principal who authorized this agent
	DelegatingPrincipal *Principal `json:"delegating_principal,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PrincipalOrErr returns the Principal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AgentEdges) PrincipalOrErr() (*Principal, error) {
	if e.Principal != nil {
		return e.Principal, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: principal.Label}
	}
	return nil, &NotLoadedError{edge: "principal"}
}

// DelegatingPrincipalOrErr returns the DelegatingPrincipal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AgentEdges) DelegatingPrincipalOrErr() (*Principal, error) {
	if e.DelegatingPrincipal != nil {
		return e.DelegatingPrincipal, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: principal.Label}
	}
	return nil, &NotLoadedError{edge: "delegating_principal"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Agent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case agent.FieldDelegatingPrincipalID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case agent.FieldCapabilityConstraints, agent.FieldResourceConstraints:
			values[i] = new([]byte)
		case agent.FieldRequiresConfirmation:
			values[i] = new(sql.NullBool)
		case agent.FieldMaxTokenLifetime:
			values[i] = new(sql.NullInt64)
		case agent.FieldModelID, agent.FieldVersion, agent.FieldSessionID:
			values[i] = new(sql.NullString)
		case agent.FieldCreatedAt, agent.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case agent.FieldID, agent.FieldPrincipalID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Agent fields.
func (_m *Agent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case agent.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case agent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case agent.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case agent.FieldPrincipalID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field principal_id", values[i])
			} else if value != nil {
				_m.PrincipalID = *value
			}
		case agent.FieldModelID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field model_id", values[i])
			} else if value.Valid {
				_m.ModelID = value.String
			}
		case agent.FieldVersion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = value.String
			}
		case agent.FieldDelegatingPrincipalID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field delegating_principal_id", values[i])
			} else if value.Valid {
				_m.DelegatingPrincipalID = new(uuid.UUID)
				*_m.DelegatingPrincipalID = *value.S.(*uuid.UUID)
			}
		case agent.FieldCapabilityConstraints:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field capability_constraints", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.CapabilityConstraints); err != nil {
					return fmt.Errorf("unmarshal field capability_constraints: %w", err)
				}
			}
		case agent.FieldResourceConstraints:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field resource_constraints", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.ResourceConstraints); err != nil {
					return fmt.Errorf("unmarshal field resource_constraints: %w", err)
				}
			}
		case agent.FieldMaxTokenLifetime:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_token_lifetime", values[i])
			} else if value.Valid {
				_m.MaxTokenLifetime = int(value.Int64)
			}
		case agent.FieldSessionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value.Valid {
				_m.SessionID = new(string)
				*_m.SessionID = value.String
			}
		case agent.FieldRequiresConfirmation:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field requires_confirmation", values[i])
			} else if value.Valid {
				_m.RequiresConfirmation = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Agent.
// This includes values selected through modifiers, order, etc.
func (_m *Agent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPrincipal queries the "principal" edge of the Agent entity.
func (_m *Agent) QueryPrincipal() *PrincipalQuery {
	return NewAgentClient(_m.config).QueryPrincipal(_m)
}

// QueryDelegatingPrincipal queries the "delegating_principal" edge of the Agent entity.
func (_m *Agent) QueryDelegatingPrincipal() *PrincipalQuery {
	return NewAgentClient(_m.config).QueryDelegatingPrincipal(_m)
}

// Update returns a builder for updating this Agent.
// Note that you need to call Agent.Unwrap() before calling this method if this Agent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Agent) Update() *AgentUpdateOne {
	return NewAgentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Agent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Agent) Unwrap() *Agent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Agent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Agent) String() string {
	var builder strings.Builder
	builder.WriteString("Agent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("principal_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.PrincipalID))
	builder.WriteString(", ")
	builder.WriteString("model_id=")
	builder.WriteString(_m.ModelID)
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(_m.Version)
	builder.WriteString(", ")
	if v := _m.DelegatingPrincipalID; v != nil {
		builder.WriteString("delegating_principal_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("capability_constraints=")
	builder.WriteString(fmt.Sprintf("%v", _m.CapabilityConstraints))
	builder.WriteString(", ")
	builder.WriteString("resource_constraints=")
	builder.WriteString(fmt.Sprintf("%v", _m.ResourceConstraints))
	builder.WriteString(", ")
	builder.WriteString("max_token_lifetime=")
	builder.WriteString(fmt.Sprintf("%v", _m.MaxTokenLifetime))
	builder.WriteString(", ")
	if v := _m.SessionID; v != nil {
		builder.WriteString("session_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("requires_confirmation=")
	builder.WriteString(fmt.Sprintf("%v", _m.RequiresConfirmation))
	builder.WriteByte(')')
	return builder.String()
}

// Agents is a parsable slice of Agent.
type Agents []*Agent
