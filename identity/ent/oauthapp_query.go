// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/grokify/systemforge/identity/ent/oauthapp"
	"github.com/grokify/systemforge/identity/ent/oauthappsecret"
	"github.com/grokify/systemforge/identity/ent/oauthauthcode"
	"github.com/grokify/systemforge/identity/ent/oauthconsent"
	"github.com/grokify/systemforge/identity/ent/oauthtoken"
	"github.com/grokify/systemforge/identity/ent/organization"
	"github.com/grokify/systemforge/identity/ent/predicate"
	"github.com/grokify/systemforge/identity/ent/user"
)

// OAuthAppQuery is the builder for querying OAuthApp entities.
type OAuthAppQuery struct {
	config
	ctx              *QueryContext
	order            []oauthapp.OrderOption
	inters           []Interceptor
	predicates       []predicate.OAuthApp
	withOwner        *UserQuery
	withOrganization *OrganizationQuery
	withSecrets      *OAuthAppSecretQuery
	withTokens       *OAuthTokenQuery
	withAuthCodes    *OAuthAuthCodeQuery
	withConsents     *OAuthConsentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OAuthAppQuery builder.
func (_q *OAuthAppQuery) Where(ps ...predicate.OAuthApp) *OAuthAppQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *OAuthAppQuery) Limit(limit int) *OAuthAppQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *OAuthAppQuery) Offset(offset int) *OAuthAppQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *OAuthAppQuery) Unique(unique bool) *OAuthAppQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *OAuthAppQuery) Order(o ...oauthapp.OrderOption) *OAuthAppQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOwner chains the current query on the "owner" edge.
func (_q *OAuthAppQuery) QueryOwner() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oauthapp.OwnerTable, oauthapp.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *OAuthAppQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oauthapp.OrganizationTable, oauthapp.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySecrets chains the current query on the "secrets" edge.
func (_q *OAuthAppQuery) QuerySecrets() *OAuthAppSecretQuery {
	query := (&OAuthAppSecretClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(oauthappsecret.Table, oauthappsecret.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oauthapp.SecretsTable, oauthapp.SecretsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTokens chains the current query on the "tokens" edge.
func (_q *OAuthAppQuery) QueryTokens() *OAuthTokenQuery {
	query := (&OAuthTokenClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(oauthtoken.Table, oauthtoken.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oauthapp.TokensTable, oauthapp.TokensColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAuthCodes chains the current query on the "auth_codes" edge.
func (_q *OAuthAppQuery) QueryAuthCodes() *OAuthAuthCodeQuery {
	query := (&OAuthAuthCodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(oauthauthcode.Table, oauthauthcode.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oauthapp.AuthCodesTable, oauthapp.AuthCodesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryConsents chains the current query on the "consents" edge.
func (_q *OAuthAppQuery) QueryConsents() *OAuthConsentQuery {
	query := (&OAuthConsentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthapp.Table, oauthapp.FieldID, selector),
			sqlgraph.To(oauthconsent.Table, oauthconsent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, oauthapp.ConsentsTable, oauthapp.ConsentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OAuthApp entity from the query.
// Returns a *NotFoundError when no OAuthApp was found.
func (_q *OAuthAppQuery) First(ctx context.Context) (*OAuthApp, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oauthapp.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *OAuthAppQuery) FirstX(ctx context.Context) *OAuthApp {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OAuthApp ID from the query.
// Returns a *NotFoundError when no OAuthApp ID was found.
func (_q *OAuthAppQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oauthapp.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *OAuthAppQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OAuthApp entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OAuthApp entity is found.
// Returns a *NotFoundError when no OAuthApp entities are found.
func (_q *OAuthAppQuery) Only(ctx context.Context) (*OAuthApp, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oauthapp.Label}
	default:
		return nil, &NotSingularError{oauthapp.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *OAuthAppQuery) OnlyX(ctx context.Context) *OAuthApp {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OAuthApp ID in the query.
// Returns a *NotSingularError when more than one OAuthApp ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *OAuthAppQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oauthapp.Label}
	default:
		err = &NotSingularError{oauthapp.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *OAuthAppQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OAuthApps.
func (_q *OAuthAppQuery) All(ctx context.Context) ([]*OAuthApp, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OAuthApp, *OAuthAppQuery]()
	return withInterceptors[[]*OAuthApp](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *OAuthAppQuery) AllX(ctx context.Context) []*OAuthApp {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OAuthApp IDs.
func (_q *OAuthAppQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(oauthapp.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *OAuthAppQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *OAuthAppQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*OAuthAppQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *OAuthAppQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *OAuthAppQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *OAuthAppQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OAuthAppQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *OAuthAppQuery) Clone() *OAuthAppQuery {
	if _q == nil {
		return nil
	}
	return &OAuthAppQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]oauthapp.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.OAuthApp{}, _q.predicates...),
		withOwner:        _q.withOwner.Clone(),
		withOrganization: _q.withOrganization.Clone(),
		withSecrets:      _q.withSecrets.Clone(),
		withTokens:       _q.withTokens.Clone(),
		withAuthCodes:    _q.withAuthCodes.Clone(),
		withConsents:     _q.withConsents.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithOwner(opts ...func(*UserQuery)) *OAuthAppQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOwner = query
	return _q
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithOrganization(opts ...func(*OrganizationQuery)) *OAuthAppQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithSecrets tells the query-builder to eager-load the nodes that are connected to
// the "secrets" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithSecrets(opts ...func(*OAuthAppSecretQuery)) *OAuthAppQuery {
	query := (&OAuthAppSecretClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSecrets = query
	return _q
}

// WithTokens tells the query-builder to eager-load the nodes that are connected to
// the "tokens" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithTokens(opts ...func(*OAuthTokenQuery)) *OAuthAppQuery {
	query := (&OAuthTokenClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTokens = query
	return _q
}

// WithAuthCodes tells the query-builder to eager-load the nodes that are connected to
// the "auth_codes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithAuthCodes(opts ...func(*OAuthAuthCodeQuery)) *OAuthAppQuery {
	query := (&OAuthAuthCodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAuthCodes = query
	return _q
}

// WithConsents tells the query-builder to eager-load the nodes that are connected to
// the "consents" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *OAuthAppQuery) WithConsents(opts ...func(*OAuthConsentQuery)) *OAuthAppQuery {
	query := (&OAuthConsentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withConsents = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OAuthApp.Query().
//		GroupBy(oauthapp.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *OAuthAppQuery) GroupBy(field string, fields ...string) *OAuthAppGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OAuthAppGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = oauthapp.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//	}
//
//	client.OAuthApp.Query().
//		Select(oauthapp.FieldClientID).
//		Scan(ctx, &v)
func (_q *OAuthAppQuery) Select(fields ...string) *OAuthAppSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &OAuthAppSelect{OAuthAppQuery: _q}
	sbuild.label = oauthapp.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OAuthAppSelect configured with the given aggregations.
func (_q *OAuthAppQuery) Aggregate(fns ...AggregateFunc) *OAuthAppSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *OAuthAppQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !oauthapp.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *OAuthAppQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OAuthApp, error) {
	var (
		nodes       = []*OAuthApp{}
		_spec       = _q.querySpec()
		loadedTypes = [6]bool{
			_q.withOwner != nil,
			_q.withOrganization != nil,
			_q.withSecrets != nil,
			_q.withTokens != nil,
			_q.withAuthCodes != nil,
			_q.withConsents != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OAuthApp).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OAuthApp{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOwner; query != nil {
		if err := _q.loadOwner(ctx, query, nodes, nil,
			func(n *OAuthApp, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *OAuthApp, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSecrets; query != nil {
		if err := _q.loadSecrets(ctx, query, nodes,
			func(n *OAuthApp) { n.Edges.Secrets = []*OAuthAppSecret{} },
			func(n *OAuthApp, e *OAuthAppSecret) { n.Edges.Secrets = append(n.Edges.Secrets, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTokens; query != nil {
		if err := _q.loadTokens(ctx, query, nodes,
			func(n *OAuthApp) { n.Edges.Tokens = []*OAuthToken{} },
			func(n *OAuthApp, e *OAuthToken) { n.Edges.Tokens = append(n.Edges.Tokens, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAuthCodes; query != nil {
		if err := _q.loadAuthCodes(ctx, query, nodes,
			func(n *OAuthApp) { n.Edges.AuthCodes = []*OAuthAuthCode{} },
			func(n *OAuthApp, e *OAuthAuthCode) { n.Edges.AuthCodes = append(n.Edges.AuthCodes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withConsents; query != nil {
		if err := _q.loadConsents(ctx, query, nodes,
			func(n *OAuthApp) { n.Edges.Consents = []*OAuthConsent{} },
			func(n *OAuthApp, e *OAuthConsent) { n.Edges.Consents = append(n.Edges.Consents, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *OAuthAppQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OAuthApp)
	for i := range nodes {
		fk := nodes[i].OwnerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "owner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *OAuthAppQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*OAuthApp)
	for i := range nodes {
		if nodes[i].OrganizationID == nil {
			continue
		}
		fk := *nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *OAuthAppQuery) loadSecrets(ctx context.Context, query *OAuthAppSecretQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *OAuthAppSecret)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OAuthApp)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oauthappsecret.FieldAppID)
	}
	query.Where(predicate.OAuthAppSecret(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oauthapp.SecretsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AppID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "app_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OAuthAppQuery) loadTokens(ctx context.Context, query *OAuthTokenQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *OAuthToken)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OAuthApp)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oauthtoken.FieldAppID)
	}
	query.Where(predicate.OAuthToken(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oauthapp.TokensColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AppID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "app_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OAuthAppQuery) loadAuthCodes(ctx context.Context, query *OAuthAuthCodeQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *OAuthAuthCode)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OAuthApp)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oauthauthcode.FieldAppID)
	}
	query.Where(predicate.OAuthAuthCode(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oauthapp.AuthCodesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AppID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "app_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *OAuthAppQuery) loadConsents(ctx context.Context, query *OAuthConsentQuery, nodes []*OAuthApp, init func(*OAuthApp), assign func(*OAuthApp, *OAuthConsent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*OAuthApp)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(oauthconsent.FieldAppID)
	}
	query.Where(predicate.OAuthConsent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(oauthapp.ConsentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AppID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "app_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *OAuthAppQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *OAuthAppQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oauthapp.Table, oauthapp.Columns, sqlgraph.NewFieldSpec(oauthapp.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oauthapp.FieldID)
		for i := range fields {
			if fields[i] != oauthapp.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOwner != nil {
			_spec.Node.AddColumnOnce(oauthapp.FieldOwnerID)
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(oauthapp.FieldOrganizationID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *OAuthAppQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(oauthapp.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = oauthapp.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OAuthAppGroupBy is the group-by builder for OAuthApp entities.
type OAuthAppGroupBy struct {
	selector
	build *OAuthAppQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *OAuthAppGroupBy) Aggregate(fns ...AggregateFunc) *OAuthAppGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *OAuthAppGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OAuthAppQuery, *OAuthAppGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *OAuthAppGroupBy) sqlScan(ctx context.Context, root *OAuthAppQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OAuthAppSelect is the builder for selecting fields of OAuthApp entities.
type OAuthAppSelect struct {
	*OAuthAppQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *OAuthAppSelect) Aggregate(fns ...AggregateFunc) *OAuthAppSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *OAuthAppSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OAuthAppQuery, *OAuthAppSelect](ctx, _s.OAuthAppQuery, _s, _s.inters, v)
}

func (_s *OAuthAppSelect) sqlScan(ctx context.Context, root *OAuthAppQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
